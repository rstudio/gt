# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: false
cache: packages

jobs:
  include:
  - r: release
  - r: devel
  - r: 3.4
  - r: 3.3
  - r: 3.2

r_binary_packages:
  - covr

after_success:
  - Rscript -e 'covr::codecov()'
