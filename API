blocks_arrange(data, groups)
cols_align(data, align = "center", columns = NULL)
cols_align_center(data, columns = NULL)
cols_align_left(data, columns = NULL)
cols_align_right(data, columns = NULL)
cols_label(data, labels)
cols_move(data, columns, after)
cols_move_to_end(data, columns)
cols_move_to_start(data, columns)
cols_remove(data, columns)
columns_with(pattern)
fa_icon(name, height = 30, fill = NULL)
fmt_currency(data, columns, rows = NULL, currency, use_subunits = TRUE, decimals = NULL, sep_mark = "", dec_mark = ".", placement = "left", negative_style = "signed")
fmt_date(data, columns, rows = NULL, date_style = NULL)
fmt_datetime(data, columns, rows = NULL, date_style = NULL, time_style = NULL)
fmt_missing(data, missing_mark = "---")
fmt_number(data, columns, rows = NULL, decimals = NULL, sep_mark = "", dec_mark = ".", drop0trailing = FALSE, negative_style = "signed")
fmt_percent(data, columns, rows = NULL, decimals = NULL, sep_mark = "", dec_mark = ".", negative_style = "signed", drop0trailing = FALSE)
fmt_scientific(data, columns, rows = NULL, decimals = NULL, sep_mark = "", dec_mark = ".", negative_style = "signed", drop0trailing = FALSE)
fmt_time(data, columns, rows = NULL, time_style = NULL)
ggplot_image(plot_object, height = 30, x = 7, y = 5)
gt(data, rownames_to_stub = FALSE, split_cols_dots = FALSE)
html(text)
html_dependency_gt_css()
local_image(file, height = 30)
local_svg(file, height = 30)
md(text)
not_in_group()
rownames_with(pattern)
tab_boxhead_panel(data, spanner, columns)
tab_footnote(data, footnote, location)
tab_heading(data, title, headnote = NULL)
tab_source_note(data, source_note)
tab_stub_block(data, group, rows, others = NULL)
tab_stubhead_caption(data, caption)
target_cell(row = NULL, column = NULL)
web_image(url, height = 30)
