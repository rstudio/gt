apply_styles(bkgd_color = NULL, text_color = NULL, text_font = NULL, text_style = NULL, text_size = NULL, text_align = NULL, text_indent = NULL, text_decorate = NULL, text_transform = NULL)
as_raw_html(data, inline_css = TRUE)
blocks_arrange(data, groups)
col_labels(...)
cols_align(data, align = "center", columns)
cols_align_center(data, columns)
cols_align_left(data, columns)
cols_align_right(data, columns)
cols_label(data, labels)
cols_merge(data, col_1, col_2, pattern = "{1} {2}")
cols_merge_range(data, col_begin, col_end)
cols_merge_uncert(data, col_val, col_uncert)
cols_move(data, columns, after)
cols_move_to_end(data, columns)
cols_move_to_start(data, columns)
cols_remove(data, columns)
cols_split_delim(data, delim, columns = NULL)
columns_with(pattern)
data_cells(rows = NULL, columns = NULL)
fa_icon(name, height = 30, fill = NULL)
fmt(data, columns, rows = TRUE, fcns)
fmt_currency(data, columns, rows = NULL, currency, use_subunits = TRUE, negative_val = "signed", decimals = NULL, use_seps = TRUE, scale_by = 1, pattern = "{x}", sep_mark = ",", dec_mark = ".", placement = "left", locale = NULL)
fmt_date(data, columns, rows = NULL, date_style)
fmt_datetime(data, columns, rows = NULL, date_style, time_style)
fmt_missing(data, columns, rows = NULL, missing_text = "---")
fmt_number(data, columns, rows = NULL, decimals = 2, drop_trailing_zeros = FALSE, negative_val = "signed", use_seps = TRUE, scale_by = 1, pattern = "{x}", sep_mark = ",", dec_mark = ".", locale = NULL)
fmt_percent(data, columns, rows = NULL, decimals = 2, drop_trailing_zeros = FALSE, negative_val = "signed", use_seps = TRUE, pattern = "{x}", sep_mark = ",", dec_mark = ".", incl_space = FALSE, placement = "right", locale = NULL)
fmt_scientific(data, columns, rows = NULL, decimals = 2, drop_trailing_zeros = FALSE, scale_by = 1, pattern = "{x}", sep_mark = ",", dec_mark = ".", locale = NULL)
fmt_time(data, columns, rows = NULL, time_style)
ggplot_image(plot_object, height = 100, aspect_ratio = 1)
gt(data, rownames_to_stub = FALSE)
gt_preview(data, rownames_to_stub = FALSE, top_n = 5, bottom_n = 1)
html(text)
html_dependency_gt_css()
info_date_style()
info_time_style()
local_image(file, height = 30)
local_svg(file, height = 30)
md(text)
not_in_group()
rownames_with(pattern)
rows_where(conditions)
summary_rows(data, groups = NULL, columns = NULL, agg, decimals = 2, sep_mark = "", dec_mark = ".", tint = NULL)
tab_boxhead_panel(data, group, columns)
tab_footnote(data, footnote, location)
tab_heading(data, title, headnote = NULL)
tab_options(data, table.background.color = NULL, table.width = NULL, heading.background.color = NULL, heading.title.font.size = NULL, heading.title.background.color = NULL, heading.headnote.font.size = NULL, heading.headnote.background.color = NULL, heading.border.top = NULL, heading.border.bottom = NULL, stub_heading.background.color = NULL, stub_heading.border.top = NULL, stub_heading.border.bottom = NULL, stub_heading_field.background.color = NULL, stub_heading_field.border.top = NULL, stub_heading_field.border.bottom = NULL, field.border.top = NULL, field.border.bottom = NULL, row.padding = NULL, row.margin = NULL, row.striping.background.color = NULL, row.striping.include.stub = NULL, row.striping.include_field = NULL, summary_row.background.color = NULL, summary_row.padding = NULL, summary_row.text_transform = NULL, footnote.font.size = NULL, footnote.padding = NULL, sourcenote.font.size = NULL, sourcenote.padding = NULL)
tab_source_note(data, source_note)
tab_stub_block(data, group, rows, others = NULL)
tab_stubhead_caption(data, caption)
tab_style(data, style, location)
web_image(url, height = 30)
write_rtf(data, file)
