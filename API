add_summary(html_tbl, group, columns, fcns, summary_labels)
cells(row, column)
cols_align_center(html_tbl, columns, types)
cols_align_left(html_tbl, columns, types)
cols_align_right(html_tbl, columns, types)
cols_move(html_tbl, columns, after)
cols_move_to_end(html_tbl, columns)
cols_move_to_start(html_tbl, columns)
cols_remove(html_tbl, columns)
cols_rename(html_tbl, ...)
cols_reorder(html_tbl, columns)
fa_icon(name, height = 30, fill)
fmt_currency(html_tbl, columns, currency, use_subunits = TRUE, decimals, use_big_mark = FALSE, negative_style = "signed")
fmt_date(html_tbl, columns, date_style)
fmt_datetime(html_tbl, columns, date_style, time_style)
fmt_font(html_tbl, columns, types, font)
fmt_fraction(html_tbl, columns, accuracy = 3)
fmt_html(html_tbl, columns)
fmt_number(html_tbl, columns, decimals, use_big_mark = FALSE, drop_trailing_zeros = FALSE, negative_style = "signed")
fmt_percent(html_tbl, columns, decimals, use_big_mark = FALSE, drop_trailing_zeros = FALSE, negative_style = "signed")
fmt_scientific(html_tbl, columns, decimals, drop_trailing_zeros = FALSE, negative_style = "signed")
fmt_time(html_tbl, columns, time_style)
fmt_uncertainty(html_tbl, value_column, uncert_column)
get_html(html_tbl)
ggplot_image(plot_object, height = 30, x = 7, y = 5)
html(text)
local_image(file, height = 30)
local_svg(file, height = 30)
md(text)
replace_missing(html_tbl, columns, types, replacement)
rownames_with(pattern)
tab_boxhead_panel(html_tbl, ..., use_names = FALSE)
tab_create(tbl, use_font_defaults = TRUE)
tab_footnote(html_tbl, glyph = "number", ...)
tab_heading(html_tbl, title, headnote, table_number, font)
tab_source_note(html_tbl, source_note, font)
tab_stub(html_tbl, column)
tab_stub_block(html_tbl, ..., .default)
tab_stubhead_caption(html_tbl, caption, alignment)
theme_classical(html_tbl)
theme_horizontal(html_tbl)
theme_schematic(html_tbl)
theme_striped(html_tbl)
web_image(url, height = 30)
