% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{as_typst}
\alias{as_typst}
\title{Output a \strong{gt} object as Typst}
\usage{
as_typst(data, container = c("auto", "table", "figure"))
}
\arguments{
\item{data}{\emph{The gt table data object}

\verb{obj:<gt_tbl>} // \strong{required}

This is the \strong{gt} table object that is commonly created through use of the
\code{\link[=gt]{gt()}} function.}

\item{container}{\emph{Top-level Typst container}

\verb{singl-kw:[auto|table|figure]} // \emph{default:} \code{"auto"}

Determines the top-level Typst construct used for the export. With
\code{"auto"}, a table caption supplied through \code{\link[=tab_caption]{tab_caption()}} causes the table
to be wrapped in a \code{figure(...)} with \code{kind: table}; otherwise, a bare
\code{table(...)} is emitted. Use \code{"table"} to always emit a bare table, or
\code{"figure"} to always emit a figure-wrapped table.}
}
\description{
Get the Typst content from a \code{gt_tbl} object as a single-element character
vector. This object can be placed inside a raw Typst block (e.g.,
\verb{```\{=typst\}} in Quarto) or written to a \code{.typ} file for inclusion
in a Typst document.
}
\section{Examples}{


Use a subset of the \code{\link{gtcars}} dataset to create a \strong{gt} table. Add a header
with \code{\link[=tab_header]{tab_header()}} and then export the table as Typst code using the
\code{as_typst()} function.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{tab_typst <-
  gtcars |>
  dplyr::select(mfr, model, msrp) |>
  dplyr::slice(1:5) |>
  gt() |>
  tab_header(
    title = md("Data listing from **gtcars**"),
    subtitle = md("`gtcars` is an R dataset")
  ) |>
  as_typst()
}\if{html}{\out{</div>}}

What's returned is a single-element vector containing the Typst code for the
table and any associated notes.
}

\section{Function ID}{

13-6
}

\section{Function Introduced}{

\code{v1.0.0} (March 26, 2026)
}

\seealso{
Other table export functions: 
\code{\link{as_gtable}()},
\code{\link{as_latex}()},
\code{\link{as_raw_html}()},
\code{\link{as_rtf}()},
\code{\link{as_word}()},
\code{\link{extract_body}()},
\code{\link{extract_cells}()},
\code{\link{extract_summary}()},
\code{\link{gtsave}()}
}
\concept{table export functions}
