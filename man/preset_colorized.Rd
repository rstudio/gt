% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/presets.R
\name{preset_colorized}
\alias{preset_colorized}
\title{Use a preset table that has a colorful look}
\usage{
preset_colorized(data, style = 1, color = "blue")
}
\arguments{
\item{data}{A table object that is created using the \code{\link[=gt]{gt()}} function.}

\item{style}{Six numbered styles are available. Simply provide a number from
\code{1} (the default) to \code{6} to choose a distinct look.}

\item{color}{There are seven color variations: \code{"blue"} (the default),
\code{"orange"}, \code{"aqua"}, \code{"magenta"}, \code{"green"}, \code{"red"}, and \code{"gray"}.}
}
\value{
an object of class \code{gt_tbl}.
}
\description{
This preset function applies a set of styles related to background colors,
line colors, and line styles. There are six \code{style}s to choose from, they
largely vary in the extent of coloring applied to different locations. Some
have table borders applied, some apply darker colors to the table stub and
summary sections, and, some even have vertical lines. In addition to \code{style},
there are seven \code{color} variations that each use a range of five color tints.
Each of the color tints have been fine-tuned to maximize the contrast between
text and its background. In total, There are 42 combinations of \code{style} and
\code{color}.
}
\section{Figures}{

\if{html}{\figure{man_preset_colorized_1.svg}{options: width=100\%}}
}

\examples{
# Use `exibble` to create a gt table with
# a number of table parts added; then, use
# the `preset_colorized()` function to give
# the table some additional style (using
# the `"aqua"` color variation and style
# number `6`)
tab_1 <-
  exibble \%>\%
  gt(rowname_col = "row", groupname_col = "group") \%>\%
  summary_rows(
    groups = "grp_a",
    columns = vars(num, currency),
    fns = list(
      min = ~min(., na.rm = TRUE),
      max = ~max(., na.rm = TRUE)
    )) \%>\%
  grand_summary_rows(
    columns = vars(currency),
    fns = list(
      total = ~sum(., na.rm = TRUE)
    )) \%>\%
  tab_source_note(source_note = "This is a source note.") \%>\%
  tab_footnote(
    footnote = "This is a footnote.",
    locations = cells_data(columns = 1, rows = 1)
  ) \%>\%
  tab_header(
    title = "The title of the table",
    subtitle = "The table's subtitle"
  ) \%>\%
  preset_colorized(style = 6, color = "aqua")

}
\seealso{
Other table preset functions: \code{\link{preset_lineless}}
}
\concept{table preset functions}
